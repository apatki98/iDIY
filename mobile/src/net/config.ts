// Server connection config — fill in your Mac's local IP before running
// Find it with: ifconfig | grep "inet " | grep -v 127.0.0.1
export const SERVER_URL = 'ws://192.168.x.x:3001';

// Hardcoded device for demo — matches the ingested Wayfair dresser manual
export const DEVICE_ID = 'IKEA-MALM-AA2301456';
